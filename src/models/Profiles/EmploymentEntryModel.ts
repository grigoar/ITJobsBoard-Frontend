export type EmploymentEntryModel = {
  title: string;
  company: string;
  startYear: string;
  endYear: string;
  description?: string;
  // startYear: number;
  // endYear: number;
  url?: string;
  indexOrder?: number;
};
