import FallbackWrapper from '@/components/Fallback/FallbackWrapper';
import React from 'react';

const FourOfFourPage = () => {
  // changeBodyTheme(constants.THEME_DEFAULT);
  // useEffect(() => {
  //   const userTheme = loadThemeLS();
  //   changeBodyTheme(userTheme);
  // }, []);

  return <FallbackWrapper />;
};

export default FourOfFourPage;
